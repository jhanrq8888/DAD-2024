export class Product {
    id?: number;
    nombre?: string;            // Nombre de la categoría
    modelo?: string;            // Modelo relacionado con la categoría
    codigo?: number;            // Código único para la categoría
    imagen?: Uint8Array;        // Imagen almacenada como un array de bytes
}
